# Configurações de ambiente para produção
# Copie este arquivo para .env e configure os valores

# Django Settings
SECRET_KEY=your-secret-key-here-change-this-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Database (SQLite é padrão, pode mudar para PostgreSQL/MySQL)
DATABASE_URL=sqlite:///db.sqlite3

# Para produção use PostgreSQL ou MySQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/rpg_db
# DATABASE_URL=mysql://user:password@localhost:3306/rpg_db

# CORS Settings (adicione URLs do frontend)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Configurações de email (opcional)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Configurações de cache (opcional)
# REDIS_URL=redis://localhost:6379/0

# Configurações de armazenamento de arquivos estáticos (opcional)
# Para AWS S3:
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=us-east-1